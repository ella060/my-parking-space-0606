# 「我的停车位」产品需求文档（PRD）

> 文档用途：记录现状 MVP + 规划下一阶段迭代方向
> 版本：v1.0 · 2026-06-06
> 项目目录：`D:\文件\【工作】\2026换工作\7 vibe code和小红书笔记\vibe code项目\0606hackson\我的停车位`

---

## 一、产品定位

**「我的停车位」**是一款面向职场人的线上情绪容纳空间。核心隐喻是：城市里有一个只属于你的集装箱停车位——下班后可以把车停在那里，在里面发呆、写日记、倾诉，或者什么都不做。它不是生产力工具，不是心理咨询，不是社交 App，只是一个"把自己放下来"的地方。

**一句话定位**：「你的线上私人停车位，不用解释，不用表现，只是在这里。」

---

## 二、目标用户

- 25–35 岁城市白领，工作强度高，情绪没有出口
- 不愿意直接向他人倾诉，更偏向独处式情绪处理
- 手机重度用户，习惯用 App 代替实体仪式（如"回家"的感觉）
- 不需要治疗，只需要一个被接住的空间

---

## 三、核心体验原则

1. **不催促，不评判**：任何动作都不应该让用户感到"我应该做更多"
2. **低摩擦进入**：主页最多 2 次点击进入空间，状态可以从上次自动恢复
3. **文字要像人说话**：所有 UI 文案用第一/第二人称，避免功能性词语
4. **画面比文字先到**：CSS-only 场景插图，无图片加载延迟，保持沉浸感
5. **退出比进入更有仪式感**：离开时应该感觉"我刚刚好好待了一会儿"

---

## 四、当前 MVP 技术栈（现状记录）

| 层 | 技术 | 说明 |
|---|---|---|
| 前端框架 | 原生 HTML + CSS + JS，无依赖 | 单文件直接浏览器打开，无构建步骤 |
| 持久化 | `localStorage`（key: `my-parking-space-state`） | 仅限单设备单浏览器，无同步 |
| 场景图形 | 纯 CSS 绘制（`div` + 渐变 + `clip-path`） | 零图片资源 |
| 动效 | CSS Animation（`rainDrop`、`breatheLine`） | 无 JS 动画 |
| 白噪音 | Web Audio API，程序生成白噪音（lowpass filter @ 760 Hz，gain 0.035） | 无音频文件 |
| AI 陪伴 | 关键词规则匹配（`companionReply` 函数，app.js:375） | 非 LLM，有安全兜底（危机关键词检测） |
| 主题切换 | CSS 自定义属性 + `data-theme` attribute（styles.css:22） | 5 套主题，纯 CSS 实现 |

---

## 五、信息架构（当前 MVP）

```
入口（首页 #homeScreen）
├── 顶栏：标题 + 实时时钟
├── 停车场场景插图（CSS 绘制：城市天际线 + 集装箱小屋 + 雨滴动效）
├── 欢迎文案（根据心情变化）
├── 心情选择条（5 个 mood-chip）
│   ├── 我很累 → 主题: 雨夜车位，入场模式: 发呆
│   ├── 我需要安静 → 主题: 雪天停车场，入场模式: 发呆
│   ├── 想发呆 → 主题: 海边集装箱，入场模式: 发呆
│   ├── 想写点什么 → 主题: 清晨小屋，入场模式: 日记（自动打开）
│   └── 想有人陪 → 主题: 深夜投影，入场模式: 倾诉（自动打开）
└── 「回到我的空间」主按钮

空间（#spaceScreen）
├── 顶部导航：「车位」返回 ｜ 主题标签 + 场景标题 ｜ 「离开」
├── 房间场景（CSS 绘制室内，7 个可点击对象）
│   ├── 窗户 → 发呆模式
│   ├── 台灯 → 切换灯光开关（视觉效果）
│   ├── 沙发 → 倾诉模式
│   ├── 日记本 → 日记模式
│   ├── 音箱 → 发呆模式（白噪音入口）
│   ├── 储物柜 → 储物箱模式
│   └── 投影仪 → 计时模式
└── 底部功能 Dock（5 按钮：发呆 / 倾诉 / 日记 / 停一会 / 布置）

模式抽屉（#modePanel，底部弹出）
├── 发呆（daze）：呼吸窗景动效 + 白噪音开关 + 「给我一句话」
├── 倾诉（chat）：类聊天气泡 + 规则引擎回复（含危机关键词兜底）
├── 日记（journal）：草稿区 + 历史列表（倒序）
├── 停一会（focus）：倒计时（5/10/18/25 分钟）+ 结束后自动向聊天写入一句话
├── 布置（decorate）：5 个主题色块切换
└── 储物箱（keepsake）：单行输入 + 时间戳列表

离开（#exitScreen）
├── 关灯的集装箱插图
├── 「车位已经替你留着」
├── 本次停留分钟数（动态计算）
└── 「回到入口」
```

---

## 六、心情 × 主题映射表（现状）

| 心情 key | 标签 | 主题 key | 主题标签 | 场景标题 | 入场模式 |
|---|---|---|---|---|---|
| tired | 我很累 | rain | 雨夜车位 | 你的小屋亮着灯 | 发呆 |
| quiet | 我需要安静 | snow | 雪天停车场 | 外面很安静 | 发呆 |
| blank | 想发呆 | sea | 海边集装箱 | 风从远处慢慢过来 | 发呆 |
| write | 想写点什么 | morning | 清晨小屋 | 窗边有一点早光 | 日记 |
| talk | 想有人陪 | cinema | 深夜投影 | 墙上留着一束光 | 倾诉 |

---

## 七、数据模型（localStorage）

```js
{
  mood: "tired",           // 当前心情 key
  theme: "rain",           // 当前主题 key（可独立于心情修改）
  lampOn: true,            // 台灯开关
  journalDraft: "",        // 日记草稿（实时持久化）
  journal: [               // 日记条目列表
    { text: string, time: "MM/DD HH:mm" }
  ],
  keepsakes: [             // 储物箱条目
    { text: string, time: "MM/DD HH:mm" }
  ],
  chat: [                  // 聊天记录（跨会话保留）
    { role: "companion" | "user", text: string }
  ],
  timer: {
    duration: 1080,        // 选定时长（秒），默认 18 分钟
    remaining: 1080,       // 当前剩余（秒）
    running: false         // 持久化时强制为 false
  }
}
```

---

## 八、陪伴者回复规则（现状）

当前是纯规则匹配，无 LLM，优先级从高到低：

1. **危机词**（死/自杀/不想活/伤害自己/撑不下去）→ 固定安全提示，建议联系紧急电话
2. 疲惫词 → 「嗯，我听见了。那我们先不解决它…」
3. 悲伤词 → 「这份难受可以先放在这里…」
4. 混乱词 → 「不用马上讲清楚…」
5. 工作词 → 「先让工作的声音留在门外…」
6. 好转词 → 「那就让这点松动多停一会儿…」
7. 兜底随机（4 条）→ 从列表随机选一条温柔回复

> `companionPrompt`（app.js:73）已定义但被 `void companionPrompt` 标记为占位符，预留给未来接入 LLM。

---

## 九、当前已知限制（MVP 边界）

| 限制 | 影响 | 备注 |
|---|---|---|
| localStorage 单设备 | 换手机/清浏览器数据后日记全丢 | MVP 可接受 |
| 无账号系统 | 无法多设备同步 | 下一阶段 |
| 陪伴者为规则引擎 | 回复固定，多次对话后感觉重复 | 下一阶段接 LLM |
| 无推送/提醒 | 用户需要主动打开才能进入空间 | 下一阶段 |
| 仅支持浏览器 | 无原生 App 体验，无法通知 | 下一阶段 |
| 无音乐/多种音效 | 只有白噪音（程序合成） | 下一阶段 |

---

## 十、下一阶段迭代建议

### P0（核心体验完善）

**1. LLM 陪伴接入**
- 目标：让「倾诉」模式的回复不再重复，有记忆感
- 方案：调用 Claude API（`claude-haiku-4-5` 成本低），`companionPrompt`（app.js:73）已写好，直接套用
- 注意：保留危机词前置过滤，LLM 只处理非危机消息
- 交互：流式输出，逐字显示，避免"等待"感

**2. 主题与心情解耦**
- 当前：选心情会强制切主题（`setMood` 里 `syncTheme=true`，app.js:129）
- 改进：布置模式修改主题后，再切心情不应覆盖用户手动选的主题
- 实现：在 state 里加 `themeOverride` 字段，`setMood` 只在 `!themeOverride` 时才同步主题

**3. 日记导出**
- 目标：用户可以把日记条目导出为 txt 文件，防止数据丢失焦虑
- 实现：`JSON.stringify` → `Blob` → `URL.createObjectURL` → `<a download>`，纯前端可实现

### P1（留存与黏性）

**4. 「今晚的停留」小结**
- 在离开页（#exitScreen）展示本次会话的行为摘要（写了几条日记、聊了多久、用了什么主题）
- 目前只显示时长，可以增加"今晚打开了白噪音""写了 2 条日记"等细节

**5. 连续打卡视觉**
- 简单的"连续 N 天来过"计数（localStorage 记录每日首次进入时间）
- 不做成「必须打卡」的压力感，只做成「你来过」的温柔记录

**6. 白噪音多样化**
- 当前：白噪音（lowpass 760 Hz）
- 增加：雨声（滤波 + 随机幅度）、海浪（调整滤波参数）、壁炉（低频振荡）
- 全部 Web Audio API 合成，无需音频文件，保持零资源依赖

**7. 储物箱分类标签**
- 目前储物箱是无结构列表
- 增加：用户可以给 keepsake 打一个 emoji 标签（如 💡 灵感 / 🌸 美好 / 💙 想留下的）
- 实现：keepsake item 加 `tag` 字段，输入框前加 emoji 选择器（最多 5 个选项，不做自由输入）

### P2（平台与传播）

**8. PWA 支持**
- `manifest.json` + Service Worker + 桌面图标
- 让用户可以"安装"到手机主屏，获得接近原生的进入仪式感
- 核心指标：离线可用（缓存 index.html + styles.css + app.js）

**9. 分享「停留记录」**
- 用户可以生成一张图片（Canvas 绘制），包含今晚停留时长、使用的主题、写下的一句话
- 不分享日记内容，只分享"我今晚在这里停了 18 分钟"这种氛围感

---

## 十一、不做清单（明确排除）

- 社交功能：不允许查看他人日记、不做好友系统
- 积分/成就/徽章：会破坏「不催促」的产品气质
- 算法推荐内容：这个空间里不应该有外部内容
- 广告变现：情绪类产品一旦有广告会立刻失去信任
- 视频/图片上传：保持纯文字+CSS场景的极简风格

---

## 十二、文案基调

所有文案遵循以下原则：
- 用「你」不用「您」
- 句子短，不超过 20 字
- 不下命令，不给建议，只是「陪着」
- 允许不完整（"几个字也可以"）
- 结尾不要感叹号

**参考用语示例**：
- ✅「今晚可以慢一点。这里没有人催你解释清楚。」
- ✅「门已经关上了，外面的声音先放在外面。」
- ✅「把今天最重的一小块放在这里。」
- ❌「欢迎使用！请选择您的当前状态。」
- ❌「您的数据已保存！」

---

## 十三、关键文件索引

| 文件 | 说明 |
|---|---|
| index.html | 3 屏 HTML 骨架 + modePanel aside，无逻辑 |
| styles.css | 1087 行，CSS 变量主题系统 + 所有场景绘制 + 动效 |
| app.js | 621 行，全部业务逻辑，无依赖，含 loadState/saveState/renderPanel/companionReply |
